# TODO (post-M14)

Nothing is in progress. Remaining work is listed as "Known gaps" in FINAL_REPORT.md and as the roadmap in docs/ROADMAP.md.
Highest value next steps:
1. Co-design and evaluation with disabled users (docs/ROADMAP.md section 1).
2. UI for signed profile export/import (the API and library exist and are tested).
3. Real-browser end-to-end tests (Playwright) and screen-reader testing.
4. Exercise the Anthropic client against the live API with a key.
5. Vendored, evaluated sound and landmark models behind the existing interfaces.
