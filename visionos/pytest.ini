[pytest]
# ".." puts the repo root on the path so `shared` imports the same way in
# tests as it will from the other four sense modules.
pythonpath = . ..
testpaths = backend/tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
